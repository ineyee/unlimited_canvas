/// 字符串常量
class ConstString {
  ConstString._internal();

  ///  工具条Widget的id
  static const toolBarWidgetId = "toolBarWidgetId";

  /// 图形层Widget的id
  static const graphicsLayerPainterId = "graphicsLayerPainterId";

  /// 笔迹层Widget的id
  static const pencilLayerPainterId = "pencilLayerPainterId";

  /// 绘制层Widget的id
  static const drawingLayerPainterId = "drawingLayerPainterId";
}
